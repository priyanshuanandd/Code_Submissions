pair<int, int> getFloorAndCeil(int arr[], int n, int x)
{

    vector<int> v(arr, arr + n);

    sort(v.begin(), v.end());

    int s = 0;
    int e = n - 1;

    int mid;

    while (e - s > 1)
    {
        mid = (e + s) / 2;

        if (v[mid] >     )
    }
}
